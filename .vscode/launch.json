{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch Package",
			"type": "go",
			"request": "launch",
			"mode": "auto",
			"program": "${workspaceRoot}/cmd/goaptcacher/",
			"env": {
				"CONFIG": "${workspaceRoot}/config.yaml",
				"CACHE_DIR": "${workspaceRoot}/cache"
			},
			"args": []
		}
	]
}
